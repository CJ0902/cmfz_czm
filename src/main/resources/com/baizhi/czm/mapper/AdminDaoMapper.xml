<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.baizhi.czm.dao.AdminDao">
    <!--登录-->
    <select id="queryuser" resultType="Admin">
        select * from c_admin where username=#{username} and password=#{password}
    </select>
    <!--查所有
    <select id="queryAll" resultType="Admin">
        select * from c_admin
    </select>-->
</mapper>